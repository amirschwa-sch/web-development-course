[project]
name = "pytest-adv"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = ["pytest"]

[tool.pytest.ini_options]
markers = [
    "functional: functional test that verify app behaivor",
    "performance: perforamnce for load-reltaed tests"
]